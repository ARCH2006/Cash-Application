# Copy this to .env and set it to wherever your backend is deployed.
# During local development this defaults to http://localhost:4000 if unset.
VITE_API_BASE_URL=https://your-backend-url.onrender.com
